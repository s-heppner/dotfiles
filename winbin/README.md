# My Windows Scripts

In this folder, I keep personal Windows-side scripts (e.g. `.bat` files).

Unlike `mybin/`, these are not deployed or added to `%PATH%` by the
deploy scripts — they're kept here purely for backup and versioning.

> [!warning]
> Do not use these yourself. They're probably horribly written.
