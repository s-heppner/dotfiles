# base16 catppuccin-mocha
# Author: https://github.com/catppuccin/catppuccin
# base16 palette. Data only: this file is sourced by the theme loader in
# `.bashrc_scripts/colors.sh` and by `base16-tmux`. It must not have side
# effects — only declare BASE00..BASE0F as 6-digit RGB hex (no leading '#').
BASE00="1e1e2e"  # Default background
BASE01="181825"  # Lighter background (status bars, line highlight)
BASE02="313244"  # Selection background
BASE03="45475a"  # Comments, invisibles, line highlight
BASE04="585b70"  # Dark foreground (status bars)
BASE05="cdd6f4"  # Default foreground
BASE06="f5e0dc"  # Light foreground
BASE07="b4befe"  # Light background
BASE08="f38ba8"  # Red   — variables, errors, diff deleted
BASE09="fab387"  # Orange — integers, constants
BASE0A="f9e2af"  # Yellow — classes, search highlight
BASE0B="a6e3a1"  # Green  — strings, diff inserted
BASE0C="94e2d5"  # Cyan   — support, escapes, regex
BASE0D="89b4fa"  # Blue   — functions, methods
BASE0E="cba6f7"  # Purple — keywords
BASE0F="f2cdcd"  # Brown  — deprecated, embedded punctuation
