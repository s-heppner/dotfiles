# base16 everforest-dark-hard
# base16 palette. Data only: this file is sourced by the theme loader in
# `.bashrc_scripts/colors.sh` and by `base16-tmux`. It must not have side
# effects — only declare BASE00..BASE0F as 6-digit RGB hex (no leading '#').
BASE00="272e33"  # Default background
BASE01="2e383c"  # Lighter background (status bars, line highlight)
BASE02="414b50"  # Selection background
BASE03="859289"  # Comments, invisibles, line highlight
BASE04="9da9a0"  # Dark foreground (status bars)
BASE05="d3c6aa"  # Default foreground
BASE06="edeada"  # Light foreground
BASE07="fffbef"  # Light background
BASE08="e67e80"  # Red   — variables, errors, diff deleted
BASE09="e69875"  # Orange — integers, constants
BASE0A="dbbc7f"  # Yellow — classes, search highlight
BASE0B="a7c080"  # Green  — strings, diff inserted
BASE0C="83c092"  # Cyan   — support, escapes, regex
BASE0D="7fbbb3"  # Blue   — functions, methods
BASE0E="d699b6"  # Purple — keywords
BASE0F="9da9a0"  # Brown  — deprecated, embedded punctuation
