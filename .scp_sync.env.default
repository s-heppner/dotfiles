# SCP_SYNC_CONFIG example file
# See `/home/.bashrc_scripts/scp_sync.sh` for details.

# SCP_SYNC_SSH_HOST: The remote host containing the data to sync
# Must be either an full Host or defined inside the SSH config
SCP_SYNC_SSH_HOST=user@remotehost.com

# SCP_SYNC_DIR_REMOTE: The directory to sync from the host machine
# Make sure that it does not have a trailing `/`!.
SCP_SYNC_DIR_REMOTE=~/workspace/sync
